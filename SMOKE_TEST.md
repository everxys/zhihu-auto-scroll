# 发布冒烟记录

日期：2026-06-17

## 自动化

- `npm run check`：通过。
- `npm test`：通过，25 个纯逻辑、DOM 策略和归档工具测试。
- `npm run test:smoke`：通过，Playwright Chromium 本地渲染冒烟。

## 渲染冒烟结果

- 页面不渲染 `#zhihu-auto-expand-panel` 和 `.zae-open-panel`：通过。
- Playwright 通过 `window.zhihuAutoExpand` API 启动、暂停和配置滚动：通过。
- 普通滚动期间暂停后不再滚动：通过。
- 底部回弹等待期间暂停后不再滚动：通过。
- 回答区和加载更多目标会点击：通过。
- 页头、侧栏、评论区同名按钮不会点击：通过。
- 新增回答增量处理：通过。
- 通过自动化 API 修改速度和执行间隔：通过。
- 200ms 执行间隔按相邻主轮次起始时间计算；接近底部时仍允许回弹流程超过该间隔。
- 回答进度通过 `snapshot.answerCount/totalAnswerCount` 输出：通过。
- 后台标签页进入等待状态，恢复后继续且不会误触发自动暂停：通过。
- 普通滚动和底部回弹始终使用平滑动画：通过。
- “展开评论”开关通过自动化 API 配置并可持久化：通过。
- 悬浮评论入口不点击，答案底部评论入口点击一次：通过。
- 评论中连续出现的“展开其他 N 条回复”会递归全部展开：通过。
- `16x / 200ms` 下等待平滑滚动结束，依次打开两个答案评论：通过。
- 评论入口首次点击无效果时停留原位重试，第二次成功后继续：通过。
- 评论入口确认评论容器出现后才完成，关闭“展开评论”会立即清理等待锁：通过。
- 长页面评论模式不会反复全页查询 `.ContentItem-actions`：通过。
- 刷新后保留自动化 API 配置，页面仍不渲染面板：通过。
- SPA 切换问题后暂停并重置、离开问题页后保持无面板：通过。
- 浏览器控制插件访问本地冒烟页被客户端策略拦截，已按测试技能约定改用 Playwright Chromium。

## 当前知乎页面结构核对

公开问题 `https://www.zhihu.com/question/19550517` 检查结果：

- `.Question-main`：存在。
- `.QuestionAnswers-answers`：存在。
- `.Question-mainColumn .List`：存在。
- `.AnswerItem`：存在，页面中发现 3 个。

真实长回答 `https://www.zhihu.com/question/27411568` 评论入口状态核对：

- 阅读过程中，评论操作栏为 `position: fixed`，与答案底部相距约 1700px，判定为悬浮状态。
- 到达答案底部后，评论操作栏变为 `position: static`，并与答案底部重合，判定为可点击状态。
- 未登录环境点击评论会打开登录层，因此评论追加与回复递归展开使用本地渲染冒烟验证。

未在真实知乎账户环境执行自动点击和长时间到底运行，避免对第三方站点产生非必要操作。
